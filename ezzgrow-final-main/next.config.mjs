/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "flowbite.s3.amazonaws.com",
      "images.unsplash.com",
      "assets.awwwards.com",
      "aceternity.com",
    ],
  },
};

export default nextConfig;
